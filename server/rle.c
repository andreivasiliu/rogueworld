/* RLE Compression. */


/* Encodes at most 'max' bytes into dest, including the terminating zero,
 * and returns 1 if it did not fit. */
int rle_encode( char *src, char *dest, int max )
{
   
   
   
   return 0;
}


void rle_decode( char *src, char *dest )
{
   
   
   
}

